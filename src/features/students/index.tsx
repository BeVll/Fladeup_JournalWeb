
import {CardBody, CardHeader, Tab, Tabs} from "@nextui-org/react";
import {Outlet} from "react-router-dom";
import {CustomCard} from "../../components/CustomCard.tsx";

export const Students = () => {
    return (
                <Outlet/>

    );
};
