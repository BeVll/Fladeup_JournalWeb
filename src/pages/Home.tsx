import {Header} from "../components/Header.tsx";

export const Home = () => {
    return (
        <div className="flex flex-col w-full">
            <Header pageTitle={"Home"}/>
        </div>
    );
};